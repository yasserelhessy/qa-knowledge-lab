# Demo is the default. Export variables in your shell; .env is not auto-loaded.
QA_MODE=demo
# Optional: pip install -e ".[openai]"
# QA_MODE=openai
# QA_MODEL=gpt-4.1-mini
# OPENAI_API_KEY=replace-locally
